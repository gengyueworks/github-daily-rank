# 交接：GitHub 日榜小红书流水线 · 本地直出版 · 2026-08-22

> ⚠️ **2026-08-22 深夜作废更新（必读，覆盖下文大部分内容）**：
> 1. **本地渲染路线已废弃**。用户拍板回到 **Notion 批量出图** 流水线（见 `HANDOVER_2026-08-21_小红书日榜全自动流水线.md` 的双层分工）。
> 2. **`batch_30-v2/` 是废品**（描述太短不能发），已连同旧版v1 移入 `xhs-assets/废弃-20260822/`，禁止再拿去发布。
> 3. **文字唯一权威源 = `xhs-assets/选题/30期选题清单-富化v3.md`**（90 卡全富化：定位 60~90 字 + 真实功能点 + 差异化封面标题；事实基准为 GitHub API 实时数据）。发 Notion 用这份。
> 4. 视觉已确认：封面克莱因蓝、内页白底（即 v2 模板那套配色），与 Notion 流水线模板一致。

> **本文件取代 `HANDOVER_2026-08-21_小红书日榜全自动.md`**（那版还依赖 Notion 渲染，已过时）。
> 下一个窗口只需读本文件 + `WORKFLOW.md` 第十、十一节，即可无缝接管。所有路径均为绝对路径。

## 一、当前状态一句话

30 期发布包（每期封面 + 3 张项目卡 + 文案 + zip）**已在本地全部渲染完成**，不再依赖 Notion 作图。卡壳原因（Notion 环节推不动）已被夜间会话用 PIL 本地直出根治。**剩下唯一的事：往小红书发。**

## 二、8-21 交接之后发生了什么（必读）

1. **8-22 凌晨会话**新写了 `gen_batch30_v2.py` + 富化v2 选题清单，绕过 Notion，本地直出 30 期全套图 → `batch_30-v2/`
2. **8-22 本会话**做了三件收尾：
   - 修掉 `short_hook()` 截断 bug（原来硬切 18 字会把「按50+类目」切成「按50+类」），已重跑生成 30/30 期
   - 补齐第 3 期 odysseus 的空壳定位（真实描述：Self-hosted AI workspace）
   - 抽验星标数据为真实值（openclaw 38.7万★、superpowers 27.5万★、ECC 24.2万★ 均与 GitHub 实时一致）

## 三、核心交付物（绝对路径）

| 交付物 | 路径 |
|---|---|
| **30 期发布包（成品）** | `/Volumes/拓展坞 1T2022/2 Codex-Workspace/Codex-Workspace-Main/30-项目-网站/gengyueworks-Github/github-daily-rank/xhs-assets/batch_30-v2/`（30-N.zip，N=1~30） |
| 每期内容 | cover.png + card-01~03.png + xhs_post.txt（1242×1660，3:4） |
| 选题数据源 | `/Volumes/拓展坞 1T2022/2 Codex-Workspace/Codex-Workspace-Main/30-项目-网站/gengyueworks-Github/github-daily-rank/xhs-assets/选题/30期选题清单-富化v2.md` |
| 渲染脚本（可重跑） | `/Volumes/拓展坞 1T2022/2 Codex-Workspace/Codex-Workspace-Main/30-项目-网站/gengyueworks-Github/github-daily-rank/xhs-assets/gen_batch30_v2.py` |
| 视觉权威源 | 同目录 `xhs_render.py`（PIL，白底 fable5 模板，勿改视觉规格） |
| 流水线文档 | 同仓库 `WORKFLOW.md` 第十节（生产规范）+ 第十一节 v2.1（本地直出） |
| 发布记录 | `/Volumes/拓展坞 1T2022/2 Codex-Workspace/Codex-Workspace-Main/30-项目-网站/gengyueworks-Github/github-daily-rank/xhs-assets/发布记录.md` |

**重跑命令**（改了选题清单后刷新全部图包）：
```bash
cd "/Volumes/拓展坞 1T2022/2 Codex-Workspace/Codex-Workspace-Main/30-项目-网站/gengyueworks-Github/github-daily-rank/xhs-assets" && python3 gen_batch30_v2.py
```

## 四、下一个窗口怎么干（按顺序）

### 第 1 步：发布（唯一真正待办）
- 库存：30 期成品 zip，随取随发。第 1 篇已于 2026-08-15 发过（见发布记录链接）
- 发布方式：ego-browser 开小红书创作平台上传（图片 + xhs_post.txt 正文），或用户手动发
- **tab 铁律**：每个 tab 用完立刻关，space 用完 `completeTaskSpace(keep:false)`
- 每发一期 → 在 `发布记录.md` 追加一行（日期 + 期号 + 链接 + 图产物路径）

### 第 2 步：视觉定稿（发布前建议确认一次）
- ⚠️ 当前存在**两套并存视觉**：
  - A. `xhs_render.py` 白底模板（batch_30-v2 实际用的这套）
  - B. `template_cover_v2.html` / `template_repo_v2.html` 克莱因蓝模板（WORKFLOW 十一节说「统一用 v2」，但本地直出没走它）
- 用户拍板用哪套。若选 B（克莱因蓝），需把 `xhs_render.py` 的配色改成 IKB 底白字后重跑脚本；若选 A，改掉 WORKFLOW 十一节的「统一 v2」表述

### 第 3 步：日常维护
- 每天 09:00 launchd `ghrank_daily.sh` 自动跑网站数据链路（与本栏目独立，不用管）
- 想加新一期：在富化v2 清单末尾按格式追加 `## 第 N 期` → 重跑 gen_batch30_v2.py

## 五、已知瑕疵（不阻塞发布，但要知道）

1. **判断句只有 5 句轮换**（让工具回归工具 / 省去重复造轮子 / 把选择权交还给你 / 把复杂变可复用 / 关键时能省半天），同期 3 卡共用一句。发几期没问题，长期发建议扩到 10+ 句
2. **封面标题模板化**：「今天值得看的三个开源项目，X 领衔」×30，信息流里辨识度一般
3. **图上有日期角标**（08 · 22），囤稿打乱顺序发时会穿帮；介意的话改 `gen_batch30_v2.py` 的 `date_tag` 或给 `xhs_render.py` 加去日期开关
4. **git 未提交**：`batch_30-v2/`、`gen_batch30_v2.py`、富化v2 清单均为 untracked（遵守「不主动 commit」规则，等用户指示）。注意 batch_30-v2 约 17MB 图片，commit 前先看仓库 .gitignore 是否该排除

## 六、质量验证记录（本会话已完成）

- ✅ 30 期 × (1 封面 + 3 卡) 全部生成，尺寸 1242×1660
- ✅ 截断 bug 修复后重跑，文案无半词截断
- ✅ 星标数据抽验 5 个仓库，全部与 GitHub 实时一致
- ✅ 定位字段无空壳（grep 「实用型选手」= 0）
- ⚠️ 未做：de-ai-flavor 全量扫描富化v2（08-21 版扫过通过，v2 只改了少量字段）

---
交接人：ox-alpha · 2026-08-22 · 成品就绪，只差发布
