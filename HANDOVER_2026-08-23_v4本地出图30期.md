# 交接：小红书日榜 v4 本地出图（30 期）· 2026-08-23

> 交接人：夜间巡航窗口。原窗口（小红书日榜本地渲染版交接 ses_fda85fb2dffe）12:00 起无响应瘫痪，工作流已完整还原，本文件 + 已恢复的脚本 = 全部所需。

## 一、任务一句话

用已恢复的 `gen_html_v4.py` 生成的 30 期 HTML，通过 ego-browser 截图成 PNG（1242×1660），存入 `本地渲染-v4/第N期/`。用户已批准此路线（今晨视觉对比 v3 合格品「完全一致」），指令：**先出 30 篇**。

## 二、资产位置（全部真实存在，已验证）

| 资产 | 绝对路径 |
|---|---|
| HTML 生成脚本（已恢复+路径持久化） | `/Volumes/拓展坞 1T2022/2 Codex-Workspace/Codex-Workspace-Main/30-项目-网站/gengyueworks-Github/github-daily-rank/xhs-assets/gen_html_v4.py` |
| 30 期 HTML（刚生成，每期 cover.html + card-01~03.html） | `…/github-daily-rank/xhs-assets/xhs_render_html_v4/第1期~第30期/` |
| 视觉模板（克莱因蓝封面+白底卡） | `…/xhs-assets/Notion出图包-2026-08-22/template_cover_v2.html`、`template_repo_v2.html` |
| 文字源 | `…/Notion出图包-2026-08-22/30期选题清单-富化v4.md` |
| PNG 输出目标目录 | `…/xhs-assets/本地渲染-v4/第N期/`（cover.png + card-01~03.png） |
| 质量对照标准（用户认可的 v3 合格品） | `/Users/a0302/Downloads/第1期_v3/` |

## 三、截图方法（原窗口验证过的唯一可行方式）

用 **ego-browser**（不要用 playwright，铁律）：

```bash
ego-browser nodejs <<'EOF'
const fs = require('fs')
await useOrCreateTaskSpace('xhs-local-render')
const base = '/Volumes/拓展坞 1T2022/2 Codex-Workspace/Codex-Workspace-Main/30-项目-网站/gengyueworks-Github/github-daily-rank/xhs-assets'
const enc = p => 'file://' + encodeURI(p)
// 对每一期的每个 html：
// 1. gotoAndWait 打开 file:// URL
// 2. await cdp('Emulation.setDeviceMetricsOverride', { width: 1242, height: 1660, deviceScaleFactor: 1, mobile: false })
// 3. const shot = await captureScreenshot()  → base64 → fs.writeFileSync 存 PNG
// 4. 每张存完立刻确认文件 >50KB
EOF
```

**Tab 铁律**：复用同一个 tab 用 gotoAndWait 切换页面；全部完成后 `completeTaskSpace(id, {keep:false})` 关干净。

## 四、执行顺序

1. 先出第 1 期（cover + 3 卡）→ 与 `/Users/a0302/Downloads/第1期_v3/` 对照确认视觉一致
2. 一致 → 批量出第 2~30 期（可 5 期一组推进，每组完成 ls 验证文件数和大小）
3. 全部完成 → 更新 `xhs-assets/发布记录.md` 候选清单，回报用户

## 五、背景与注意事项

- 今晨原窗口已用同法出过第 1 期并通过用户视觉验收（「布局配色字体信息层级完全一致」）；后 /tmp 被临时清理任务清空导致脚本丢失，现已恢复并把输出路径改到持久目录（不再用 /tmp）
- 用户明确想要「自动发布到小红书」（原话：能自动发布就解决大问题了）——本期先把 30 期图出完，发布另行安排
- 已知瑕疵（不阻塞）：判断句 5 句轮换、封面标题模板化、日期角标 08·22（囤稿乱序发会穿帮）
- 富化v4 清单是文字唯一权威源；`batch_30-v2/` 和 `废弃-20260822/` 是废品禁止使用
